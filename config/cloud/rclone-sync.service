[Unit]
Description=Rclone Sync Service
After=network.target

[Service]
Type=simple
Environment=HOME=%h
ExecStart=/bin/bash /usr/local/bin/sync_remote_folders.sh
Environment=DISPLAY=:0

[Install]
WantedBy=default.target